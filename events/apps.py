from django.apps import AppConfig


class EventsConfig(AppConfig):
    """
    Events app class confirguration

    Parameters:
    App configuration for events
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'events'
